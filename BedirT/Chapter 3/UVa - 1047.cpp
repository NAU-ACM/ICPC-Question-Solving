#include <utility>
#include <iostream>
#include <vector>
#include <cstring>
using namespace std;

int main (){
	int n, t, m, num, ins, val;
	int ar[20];
	while(1){
		cin >> n >> t;
		if(n == 0 && t == 0) return 0;
		for(int i = 0; i < n ; ++i) cin >> ar[i];
		cin >> m;
		for(int i = 0; i < m ; ++i){
			cin >> num;
			for(int j = 0; j < num; ++j){
				cin >> ins;
			}
			cin >> val;
		}
		
		// TOOK THE INPUT, STARTING TO COMPUTE
		

	}
}

